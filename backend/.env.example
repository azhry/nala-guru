PORT=4000
MONGO_URI=mongodb://localhost:27017/baby-math
OPENAI_API_KEY=
DASHBOARD_PIN=0000

# AI Provider Chain (priority order):
# 1. OpenAI — primary (requires OPENAI_API_KEY)
# 2. LocalProvider — offline fallback, always available, no API key needed
# Note: OpencodeProvider (CLI-based) is excluded from Docker builds
