PORT=4000
MONGO_URI=mongodb://localhost:27017/baby-math
OPENAI_API_KEY=
DASHBOARD_PIN=0000

# AI Provider Chain (priority order):
# 1. OpenAI — primary (requires OPENAI_API_KEY)
# 2. Opencode — fallback CLI provider (available on host or if opencode CLI in PATH)
# 3. LocalProvider — ultimate offline fallback, always available, no API key needed
